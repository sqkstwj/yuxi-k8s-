apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: yuxi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-nfs
  resources:
    requests:
      storage: 5Gi

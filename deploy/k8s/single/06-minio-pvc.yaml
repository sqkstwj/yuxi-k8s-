apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: yuxi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-nfs
  resources:
    requests:
      storage: 20Gi

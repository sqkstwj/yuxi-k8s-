apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: yuxi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-nfs
  resources:
    requests:
      storage: 20Gi
